# ============================================================
# SMART BUS TRACKING SYSTEM - ENVIRONMENT VARIABLES
# ============================================================
# COPY THIS FILE TO .env AND FILL IN YOUR OWN VALUES
# NEVER commit .env to GitHub — it contains secrets!
# ============================================================

# ── SERVER ──────────────────────────────────────────────────
# The port your backend server will listen on
PORT=5000

# The environment: development | production | test
NODE_ENV=development

# The full URL of your frontend (used for CORS + email links)
CLIENT_URL=http://localhost:5173

# ── DATABASE ─────────────────────────────────────────────────
# Your MongoDB Atlas connection string
# Get this from: MongoDB Atlas → Connect → Drivers → Node.js
# Format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>
MONGODB_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/smart_bus_tracking?retryWrites=true&w=majority

# ── JWT (JSON Web Token) ─────────────────────────────────────
# Secret keys used to sign tokens. MUST be long random strings.
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Access token: short-lived (used for API requests)
JWT_ACCESS_SECRET=REPLACE_WITH_64_CHAR_RANDOM_HEX_STRING_FOR_ACCESS
JWT_ACCESS_EXPIRE=24h

# Refresh token: long-lived (used to get new access tokens)
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_64_CHAR_RANDOM_HEX_STRING
JWT_REFRESH_EXPIRE=30d

# ── EMAIL (for verification + password reset) ─────────────────
# Using Gmail SMTP as example. For production, use SendGrid or Mailgun.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your.email@gmail.com
# For Gmail: use an App Password (not your real password)
# Settings → Security → 2-Step Verification → App Passwords
EMAIL_PASSWORD=your_gmail_app_password
EMAIL_FROM_NAME=Smart Bus Tracking
EMAIL_FROM_ADDRESS=noreply@smartbustracking.com

# ── RATE LIMITING ────────────────────────────────────────────
# How many requests allowed per window per IP
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ── ENCRYPTION ───────────────────────────────────────────────
# Used to hash password reset tokens before storing
CRYPTO_SECRET=REPLACE_WITH_ANOTHER_RANDOM_STRING

# ── HEAD ADMIN (used only by scripts/seed.js) ──────────────────
# Creates/upgrades one superadmin account when you run:
#   node scripts/seed.js
# Falls back to a dev default (admin@bustrack.com / Admin@1234) if unset —
# always set these for any deployment beyond local dev.
HEAD_ADMIN_NAME=System Admin
HEAD_ADMIN_EMAIL=you@yourdomain.com
HEAD_ADMIN_PHONE=9000000001
HEAD_ADMIN_PASSWORD=REPLACE_WITH_A_STRONG_PASSWORD
